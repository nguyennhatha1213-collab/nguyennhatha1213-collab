# 🔎 Database Understanding & Schema Analysis

> 📌 **Objective:** Understand what the data contains, how it is structured, and how different tables are connected before starting data cleaning and analysis.

---

## 🧭 1. Understand the Dataset

When I receive a new dataset, I first try to understand **what the data contains and how it is structured**.

Then, I check:

- 📂 Tables and their structures
- 🏷️ Columns and column names
- 🔢 Data types
- 🔑 Primary Keys
- 🔗 Foreign Keys
- 🧩 Relationships between tables
- 📊 Number of columns in each table
- ⚠️ Nullable fields

This helps me understand the database structure before moving to data profiling, data cleaning, transformation, and analysis.

---

## 🗂️ 2. Check Tables & Columns

### 🎯 Objective

First, I inspect the database schema to understand:

- 📋 What tables are available
- 🔢 How many columns each table contains
- 🏷️ Column names
- 🔠 Data types
- 📏 Character length
- 🔢 Numeric precision and scale
- ⚠️ Whether columns allow `NULL`

### 💻 SQL Query

```sql
SELECT
    TABLE_SCHEMA,
    TABLE_NAME,
    ORDINAL_POSITION,
    COLUMN_NAME,
    DATA_TYPE,
    CHARACTER_MAXIMUM_LENGTH,
    NUMERIC_PRECISION,
    NUMERIC_SCALE,
    IS_NULLABLE,
    COUNT(*) OVER(
        PARTITION BY TABLE_SCHEMA, TABLE_NAME
    ) AS TotalColumns
FROM INFORMATION_SCHEMA.COLUMNS
ORDER BY
    TABLE_SCHEMA,
    TABLE_NAME,
    ORDINAL_POSITION;